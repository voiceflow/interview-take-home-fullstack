/**
 * 1000 intents
 */
export const DB_SIZE = 'small';

/**
 * switch to large (100k intents) once small has been sufficiently optimized, MAKE SURE TO RESTART THE SERVER
 */
// export const DB_SIZE = 'large';
